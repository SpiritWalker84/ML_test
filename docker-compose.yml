services:
  app:
    build: .
    ports:
      - "8082:8082"
    env_file: .env
    environment:
      - PORT=8082
      # SQLite in /app/db so we don't mount over /app/data (model lives there)
      - DATABASE_URL=sqlite:///./db/app.db
    volumes:
      - app_db:/app/db

volumes:
  app_db:
